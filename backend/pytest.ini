[pytest]
# Run pytest from the backend/ directory: `cd backend && pytest`
testpaths = tests
# pytest ≥ 7: adds backend/ to sys.path automatically so
# `import services.xxx` and `import routes.xxx` resolve correctly.
pythonpath = .
# Output: short traceback for cleaner CI logs; verbose flag overrides
addopts = -q --tb=short
